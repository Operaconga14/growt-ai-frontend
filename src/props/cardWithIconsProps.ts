export interface CardWithIconsProps {
    styles: [{ iconStyle: string }, { cardStyle: string }, { cardBodyStyle: string }, { titleStyle: string }, { descriptionStyle: string }, { otherStyle: string }];
    texts: [{ titleText: string }, { descriptionText: string }, { iconName: string }];
}
